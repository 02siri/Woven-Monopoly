const App = () => {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Woven Monopoly</p>
        <h1>Frontend setup is ready for the first UI slice.</h1>
        <p className="hero-copy">
          Next we will wire this app to the backend and build the create game,
          simulate game, and history views.
        </p>
      </section>
    </main>
  );
};

export default App;
